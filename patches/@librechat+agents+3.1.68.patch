diff --git a/node_modules/@librechat/agents/dist/cjs/llm/bedrock/utils/message_inputs.cjs b/node_modules/@librechat/agents/dist/cjs/llm/bedrock/utils/message_inputs.cjs
index 2ab7621..937fd43 100644
--- a/node_modules/@librechat/agents/dist/cjs/llm/bedrock/utils/message_inputs.cjs
+++ b/node_modules/@librechat/agents/dist/cjs/llm/bedrock/utils/message_inputs.cjs
@@ -392,14 +392,24 @@ function convertHumanMessageToConverseMessage(msg) {
 function convertToolMessageToConverseMessage(msg) {
     const toolCallId = msg.tool_call_id;
     let content;
+    const cachePoints = [];
     if (typeof msg.content === 'string') {
         content = [{ text: msg.content }];
     }
     else if (Array.isArray(msg.content)) {
-        content = msg.content.map((block) => convertLangChainContentBlockToConverseContentBlock({
-            block,
-            onUnknown: 'passthrough',
-        }));
+        content = [];
+        for (const block of msg.content) {
+            const converted = convertLangChainContentBlockToConverseContentBlock({
+                block,
+                onUnknown: 'passthrough',
+            });
+            if ('cachePoint' in converted) {
+                cachePoints.push(converted);
+            }
+            else {
+                content.push(converted);
+            }
+        }
     }
     else {
         content = [{ text: String(msg.content) }];
@@ -413,6 +423,7 @@ function convertToolMessageToConverseMessage(msg) {
                     content: content,
                 },
             },
+            ...cachePoints,
         ],
     };
 }
diff --git a/node_modules/@librechat/agents/dist/cjs/messages/cache.cjs b/node_modules/@librechat/agents/dist/cjs/messages/cache.cjs
index 4e554d2..fef2d62 100644
--- a/node_modules/@librechat/agents/dist/cjs/messages/cache.cjs
+++ b/node_modules/@librechat/agents/dist/cjs/messages/cache.cjs
@@ -215,89 +215,104 @@ function stripBedrockCacheControl(messages) {
     }
     return updatedMessages;
 }
-/**
- * Adds Bedrock Converse API cache points to the last two messages.
- * Inserts `{ cachePoint: { type: 'default' } }` as a separate content block
- * immediately after the last text block in each targeted message.
- * Strips ALL existing cache control (both Bedrock and Anthropic formats) from all messages,
- * then adds fresh cache points to the last 2 messages in a single backward pass.
- * This ensures we don't accumulate stale cache points across multiple turns.
- * Returns a new array - only clones messages that require modification.
- * @param messages - The array of message objects.
- * @returns - A new array of message objects with cache points added.
- */
+function envNumber(name, fallback) {
+    const value = Number.parseInt(process.env[name] ?? '', 10);
+    return Number.isFinite(value) && value > 0 ? value : fallback;
+}
+function bedrockCachePoint() {
+    const ttl = process.env.BEDROCK_CACHE_POINT_TTL;
+    const cachePoint = ttl === '5m' || ttl === '1h' ? { type: 'default', ttl } : { type: 'default' };
+    return { cachePoint };
+}
+function isToolMessage(message) {
+    return ('getType' in message &&
+        typeof message.getType === 'function' &&
+        message.getType() === 'tool');
+}
+function contentSize(content) {
+    if (typeof content === 'string') {
+        return content.length;
+    }
+    if (Array.isArray(content)) {
+        return JSON.stringify(content).length;
+    }
+    return 0;
+}
+function contentWithBedrockCachePoint(content, minimumSize) {
+    if (typeof content === 'string') {
+        if (content === '' || content.length < minimumSize) {
+            return null;
+        }
+        return [
+            { type: _enum.ContentTypes.TEXT, text: content },
+            bedrockCachePoint(),
+        ];
+    }
+    if (!Array.isArray(content)) {
+        return null;
+    }
+    const workingContent = [];
+    let lastNonEmptyTextIndex = -1;
+    for (let j = 0; j < content.length; j++) {
+        const block = content[j];
+        if (isCachePoint(block)) {
+            continue;
+        }
+        const cloned = { ...block };
+        if ('cache_control' in cloned) {
+            delete cloned.cache_control;
+        }
+        const type = cloned.type;
+        if (type === _enum.ContentTypes.TEXT || type === 'text') {
+            const text = cloned.text;
+            if (text != null && text.trim() !== '') {
+                lastNonEmptyTextIndex = workingContent.length;
+            }
+        }
+        workingContent.push(cloned);
+    }
+    if (lastNonEmptyTextIndex < 0 || contentSize(workingContent) < minimumSize) {
+        return workingContent.length === content.length ? null : workingContent;
+    }
+    workingContent.splice(lastNonEmptyTextIndex + 1, 0, bedrockCachePoint());
+    return workingContent;
+}
 function addBedrockCacheControl(messages) {
     if (!Array.isArray(messages) || messages.length < 2) {
         return messages;
     }
     const updatedMessages = [...messages];
-    let messagesModified = 0;
+    const maxCachePoints = envNumber('BEDROCK_CACHE_MAX_POINTS', 4);
+    const maxToolCachePoints = envNumber('BEDROCK_CACHE_MAX_TOOL_RESULT_POINTS', 3);
+    const toolMinimumSize = envNumber('BEDROCK_CACHE_TOOL_RESULT_MIN_CHARS', 6000);
+    let cachePointsAdded = 0;
+    let toolCachePointsAdded = 0;
     for (let i = updatedMessages.length - 1; i >= 0; i--) {
         const originalMessage = updatedMessages[i];
-        const isToolMessage = 'getType' in originalMessage &&
-            typeof originalMessage.getType === 'function' &&
-            originalMessage.getType() === 'tool';
-        const content = originalMessage.content;
-        const hasArrayContent = Array.isArray(content);
-        const isEmptyString = typeof content === 'string' && content === '';
-        const needsCacheAdd = messagesModified < 2 &&
-            !isToolMessage &&
-            !isEmptyString &&
-            (typeof content === 'string' || hasArrayContent);
-        if (!needsCacheAdd && !hasArrayContent) {
+        if (cachePointsAdded >= maxCachePoints || toolCachePointsAdded >= maxToolCachePoints || !isToolMessage(originalMessage)) {
             continue;
         }
-        let workingContent;
-        let modified = false;
-        if (hasArrayContent) {
-            // Single pass: clone blocks, strip cache markers, find last
-            // non-empty text block for cache point insertion — all at once.
-            const src = content;
-            workingContent = [];
-            let lastNonEmptyTextIndex = -1;
-            for (let j = 0; j < src.length; j++) {
-                const block = src[j];
-                if (isCachePoint(block)) {
-                    modified = true;
-                    continue;
-                }
-                const cloned = { ...block };
-                if ('cache_control' in cloned) {
-                    delete cloned.cache_control;
-                    modified = true;
-                }
-                const type = cloned.type;
-                if (type === _enum.ContentTypes.TEXT || type === 'text') {
-                    const text = cloned.text;
-                    if (text != null && text.trim() !== '') {
-                        lastNonEmptyTextIndex = workingContent.length;
-                    }
-                }
-                workingContent.push(cloned);
-            }
-            if (!modified && !needsCacheAdd) {
-                continue;
-            }
-            // Insert cache point after the last non-empty text block.
-            // Skip if no cacheable text content exists (whitespace-only messages).
-            if (needsCacheAdd && lastNonEmptyTextIndex >= 0) {
-                workingContent.splice(lastNonEmptyTextIndex + 1, 0, {
-                    cachePoint: { type: 'default' },
-                });
-                messagesModified++;
-            }
+        const workingContent = contentWithBedrockCachePoint(originalMessage.content, toolMinimumSize);
+        if (!workingContent || !workingContent.some((block) => isCachePoint(block))) {
+            continue;
         }
-        else if (typeof content === 'string' && needsCacheAdd) {
-            workingContent = [
-                { type: _enum.ContentTypes.TEXT, text: content },
-                { cachePoint: { type: 'default' } },
-            ];
-            messagesModified++;
+        updatedMessages[i] = cloneMessage(originalMessage, workingContent);
+        cachePointsAdded++;
+        toolCachePointsAdded++;
+    }
+    for (let i = updatedMessages.length - 1; i >= 0; i--) {
+        const originalMessage = updatedMessages[i];
+        if (cachePointsAdded >= maxCachePoints || isToolMessage(originalMessage)) {
+            continue;
         }
-        else {
+        const workingContent = contentWithBedrockCachePoint(originalMessage.content, 1);
+        if (!workingContent) {
             continue;
         }
         updatedMessages[i] = cloneMessage(originalMessage, workingContent);
+        if (workingContent.some((block) => isCachePoint(block))) {
+            cachePointsAdded++;
+        }
     }
     return updatedMessages;
 }
diff --git a/node_modules/@librechat/agents/dist/esm/llm/bedrock/utils/message_inputs.mjs b/node_modules/@librechat/agents/dist/esm/llm/bedrock/utils/message_inputs.mjs
index 1473664..9211426 100644
--- a/node_modules/@librechat/agents/dist/esm/llm/bedrock/utils/message_inputs.mjs
+++ b/node_modules/@librechat/agents/dist/esm/llm/bedrock/utils/message_inputs.mjs
@@ -390,14 +390,24 @@ function convertHumanMessageToConverseMessage(msg) {
 function convertToolMessageToConverseMessage(msg) {
     const toolCallId = msg.tool_call_id;
     let content;
+    const cachePoints = [];
     if (typeof msg.content === 'string') {
         content = [{ text: msg.content }];
     }
     else if (Array.isArray(msg.content)) {
-        content = msg.content.map((block) => convertLangChainContentBlockToConverseContentBlock({
-            block,
-            onUnknown: 'passthrough',
-        }));
+        content = [];
+        for (const block of msg.content) {
+            const converted = convertLangChainContentBlockToConverseContentBlock({
+                block,
+                onUnknown: 'passthrough',
+            });
+            if ('cachePoint' in converted) {
+                cachePoints.push(converted);
+            }
+            else {
+                content.push(converted);
+            }
+        }
     }
     else {
         content = [{ text: String(msg.content) }];
@@ -411,6 +421,7 @@ function convertToolMessageToConverseMessage(msg) {
                     content: content,
                 },
             },
+            ...cachePoints,
         ],
     };
 }
diff --git a/node_modules/@librechat/agents/dist/esm/messages/cache.mjs b/node_modules/@librechat/agents/dist/esm/messages/cache.mjs
index 6e6ff04..758401a 100644
--- a/node_modules/@librechat/agents/dist/esm/messages/cache.mjs
+++ b/node_modules/@librechat/agents/dist/esm/messages/cache.mjs
@@ -213,89 +213,104 @@ function stripBedrockCacheControl(messages) {
     }
     return updatedMessages;
 }
-/**
- * Adds Bedrock Converse API cache points to the last two messages.
- * Inserts `{ cachePoint: { type: 'default' } }` as a separate content block
- * immediately after the last text block in each targeted message.
- * Strips ALL existing cache control (both Bedrock and Anthropic formats) from all messages,
- * then adds fresh cache points to the last 2 messages in a single backward pass.
- * This ensures we don't accumulate stale cache points across multiple turns.
- * Returns a new array - only clones messages that require modification.
- * @param messages - The array of message objects.
- * @returns - A new array of message objects with cache points added.
- */
+function envNumber(name, fallback) {
+    const value = Number.parseInt(process.env[name] ?? '', 10);
+    return Number.isFinite(value) && value > 0 ? value : fallback;
+}
+function bedrockCachePoint() {
+    const ttl = process.env.BEDROCK_CACHE_POINT_TTL;
+    const cachePoint = ttl === '5m' || ttl === '1h' ? { type: 'default', ttl } : { type: 'default' };
+    return { cachePoint };
+}
+function isToolMessage(message) {
+    return ('getType' in message &&
+        typeof message.getType === 'function' &&
+        message.getType() === 'tool');
+}
+function contentSize(content) {
+    if (typeof content === 'string') {
+        return content.length;
+    }
+    if (Array.isArray(content)) {
+        return JSON.stringify(content).length;
+    }
+    return 0;
+}
+function contentWithBedrockCachePoint(content, minimumSize) {
+    if (typeof content === 'string') {
+        if (content === '' || content.length < minimumSize) {
+            return null;
+        }
+        return [
+            { type: ContentTypes.TEXT, text: content },
+            bedrockCachePoint(),
+        ];
+    }
+    if (!Array.isArray(content)) {
+        return null;
+    }
+    const workingContent = [];
+    let lastNonEmptyTextIndex = -1;
+    for (let j = 0; j < content.length; j++) {
+        const block = content[j];
+        if (isCachePoint(block)) {
+            continue;
+        }
+        const cloned = { ...block };
+        if ('cache_control' in cloned) {
+            delete cloned.cache_control;
+        }
+        const type = cloned.type;
+        if (type === ContentTypes.TEXT || type === 'text') {
+            const text = cloned.text;
+            if (text != null && text.trim() !== '') {
+                lastNonEmptyTextIndex = workingContent.length;
+            }
+        }
+        workingContent.push(cloned);
+    }
+    if (lastNonEmptyTextIndex < 0 || contentSize(workingContent) < minimumSize) {
+        return workingContent.length === content.length ? null : workingContent;
+    }
+    workingContent.splice(lastNonEmptyTextIndex + 1, 0, bedrockCachePoint());
+    return workingContent;
+}
 function addBedrockCacheControl(messages) {
     if (!Array.isArray(messages) || messages.length < 2) {
         return messages;
     }
     const updatedMessages = [...messages];
-    let messagesModified = 0;
+    const maxCachePoints = envNumber('BEDROCK_CACHE_MAX_POINTS', 4);
+    const maxToolCachePoints = envNumber('BEDROCK_CACHE_MAX_TOOL_RESULT_POINTS', 3);
+    const toolMinimumSize = envNumber('BEDROCK_CACHE_TOOL_RESULT_MIN_CHARS', 6000);
+    let cachePointsAdded = 0;
+    let toolCachePointsAdded = 0;
     for (let i = updatedMessages.length - 1; i >= 0; i--) {
         const originalMessage = updatedMessages[i];
-        const isToolMessage = 'getType' in originalMessage &&
-            typeof originalMessage.getType === 'function' &&
-            originalMessage.getType() === 'tool';
-        const content = originalMessage.content;
-        const hasArrayContent = Array.isArray(content);
-        const isEmptyString = typeof content === 'string' && content === '';
-        const needsCacheAdd = messagesModified < 2 &&
-            !isToolMessage &&
-            !isEmptyString &&
-            (typeof content === 'string' || hasArrayContent);
-        if (!needsCacheAdd && !hasArrayContent) {
+        if (cachePointsAdded >= maxCachePoints || toolCachePointsAdded >= maxToolCachePoints || !isToolMessage(originalMessage)) {
             continue;
         }
-        let workingContent;
-        let modified = false;
-        if (hasArrayContent) {
-            // Single pass: clone blocks, strip cache markers, find last
-            // non-empty text block for cache point insertion — all at once.
-            const src = content;
-            workingContent = [];
-            let lastNonEmptyTextIndex = -1;
-            for (let j = 0; j < src.length; j++) {
-                const block = src[j];
-                if (isCachePoint(block)) {
-                    modified = true;
-                    continue;
-                }
-                const cloned = { ...block };
-                if ('cache_control' in cloned) {
-                    delete cloned.cache_control;
-                    modified = true;
-                }
-                const type = cloned.type;
-                if (type === ContentTypes.TEXT || type === 'text') {
-                    const text = cloned.text;
-                    if (text != null && text.trim() !== '') {
-                        lastNonEmptyTextIndex = workingContent.length;
-                    }
-                }
-                workingContent.push(cloned);
-            }
-            if (!modified && !needsCacheAdd) {
-                continue;
-            }
-            // Insert cache point after the last non-empty text block.
-            // Skip if no cacheable text content exists (whitespace-only messages).
-            if (needsCacheAdd && lastNonEmptyTextIndex >= 0) {
-                workingContent.splice(lastNonEmptyTextIndex + 1, 0, {
-                    cachePoint: { type: 'default' },
-                });
-                messagesModified++;
-            }
+        const workingContent = contentWithBedrockCachePoint(originalMessage.content, toolMinimumSize);
+        if (!workingContent || !workingContent.some((block) => isCachePoint(block))) {
+            continue;
         }
-        else if (typeof content === 'string' && needsCacheAdd) {
-            workingContent = [
-                { type: ContentTypes.TEXT, text: content },
-                { cachePoint: { type: 'default' } },
-            ];
-            messagesModified++;
+        updatedMessages[i] = cloneMessage(originalMessage, workingContent);
+        cachePointsAdded++;
+        toolCachePointsAdded++;
+    }
+    for (let i = updatedMessages.length - 1; i >= 0; i--) {
+        const originalMessage = updatedMessages[i];
+        if (cachePointsAdded >= maxCachePoints || isToolMessage(originalMessage)) {
+            continue;
         }
-        else {
+        const workingContent = contentWithBedrockCachePoint(originalMessage.content, 1);
+        if (!workingContent) {
             continue;
         }
         updatedMessages[i] = cloneMessage(originalMessage, workingContent);
+        if (workingContent.some((block) => isCachePoint(block))) {
+            cachePointsAdded++;
+        }
     }
     return updatedMessages;
 }
